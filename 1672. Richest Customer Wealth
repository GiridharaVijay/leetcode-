class Solution:
    def maximumWealth(self, accounts: List[List[int]]) -> int:
        a=[]
        for i in accounts:
            for j in i:
                a.append(sum(i))
        return max(a)
        
